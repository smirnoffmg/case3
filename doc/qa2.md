Вопрос №1. Как Агент должен обращаться к БД? Какой у него доступ?
Ответ: пользователь создал объекты, которые хранятся в БД, этот агент/ассистент поможет работать с таблицами.
Вопрос №2. Предполагается ли, что ассистент сам будет выполнять запрос.
Ответ: по факту нужен набросок кода (решение), которое выполняет задачу. На нашей платформе сейчас не может SQL запрос выполняться.

Сконцентрироваться на генерации запроса!

ВОпрос №3.
Ответ: у пользователя будет запрос, что он хочет получить. На вашей ответственности и решении будет влиять на то, что передавать модели/агенту

На выходе оптимальный SQL запрос, который можно вставить

ВОпрос №4. Какие ограничения на модели? Локальные, облачные?
Ответ: можно любые, главное, чтобы работало и на маленьких на моделях. 
Могут быть ограниченные мощности. до 30 млрд (?) параметров.

Мы вам дадим API, с помощью которых можно использовать модели.

Вопрос №5. Какие будут у нас доступы?
Ответ: API - вызвать модель. И то, что вам уже скинули: Скрипт по созданию БД.

Вопрос №6. ЧТо будет с самым лучшим решением?
Ответ: Мы рассмотрим вопрос об интеграции после практикума

Вопрос №7. Какое максимальное ограничение контекстного окна LLM?
Ответ: 256к токенов. миллион не стоит.


Получить оценку безопасности важно, и это не менее важно, чем получить какой-то запрос


Добрый день, участники! Я эксперт от компании GreenData, отправляю ответы на ваши вопросы:

1) DoD: только генерация или плюс исполнение?
Главное это генерация, исполнение не обязательно, но важно чтобы сгенерированный запрос был исполняемым и не вызывал ошибок при выполнении на базе
2) Что важнее — качество генерации или безопасность?
Безопасность, но качество тоже важно
3) Что входит во «вводные данные» — только метаинфа или ещё пары «текст, SQL»?
Вы сами определяете что может входить, но если хотите в модель передавать эталоны в модель то нужно их самим сделать
4) Если эталонов нет, ок если синтезируем сами?
Да
5) Тестовые таблицы будут с данными или только схема? Только схема, данные можно любые вставить но не то чтобы их было мало, минимум 1000 значений в каждой таблице
6) Уровни доступа, есть ли данные под 152-ФЗ?
Без уровней, вся база доступна для всех
7) Можно использовать внешние облачные LLM (OpenAI и пр.)?
Да, но должна быть оптимизация под маленькие модели
8) Если только локальные — какие ресурсы выделяете?
Мы можем дать API с помощью которого можно получать ответы от локальной модели
9) 40 секунд на ответ — жёсткий лимит?
60 секунд жесткий, 40 желательный
высокая скорость, дополнительный плюс к оценке